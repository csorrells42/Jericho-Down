# Dependencies

This folder is reserved for runtime tools that Jericho Down loads relative to the executable folder.

Camera preview and video recording currently use Windows Media Foundation in-process, so there is no bundled video runtime dependency.
